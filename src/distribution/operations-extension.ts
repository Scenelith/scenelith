/**
 * Optional queue projections owned by a distribution. The returned fragment
 * must end with a comma so the shared operations query remains valid.
 */
export const operationsQueueProjectionSql = "";
